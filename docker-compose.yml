services:
  node:
    build: "."
    image: sv-mongo-graphql-utils:local
    volumes:
      - "./package.json:/app/package.json"
      - "./src:/app/src"
      - "./testData:/app/testData"
      - "./testing:/app/testing"
    depends_on:
      - "mongo"
  mongo:
    image: mongo:7.0